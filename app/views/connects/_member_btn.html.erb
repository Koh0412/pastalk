<%= form_with(model: current_user.connects.build, local: true) do |f| %>
    <%= hidden_field_tag :group_id, group.id %>
    <%= f.submit 'メンバーになる', class: 'button button-purple btn-block' %>
<% end %>